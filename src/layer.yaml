includes: ['layer:basic', 'layer:apt', 'interface:ceph-mds']  # if you use any interfaces, add them here
options:
  apt:
    packages:
      - python3-pyxattr
repo: https://git.openstack.org/openstack/charm-ceph-fs
